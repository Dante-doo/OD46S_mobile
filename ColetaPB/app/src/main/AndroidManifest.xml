<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Permissão para Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".ColetaPBApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColetaPB"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31"
        tools:replace="android:allowBackup">

        <!-- Tela inicial -->
        <activity
            android:name=".LoginPage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Após login -->
        <activity
            android:name=".TruckList"
            android:exported="false"
            android:parentActivityName=".LoginPage" />

        <!-- Da lista de caminhões para rotas -->
        <activity
            android:name=".RouteList"
            android:exported="false"
            android:parentActivityName=".TruckList" />

        <!-- Da lista de rotas para iniciar rota -->
        <activity
            android:name=".StartRoute"
            android:exported="false"
            android:parentActivityName=".RouteList" />

        <!-- Lista de pontos da rota -->
        <activity
            android:name=".RoutePointsListActivity"
            android:exported="false"
            android:parentActivityName=".StartRoute" />

        <!-- Registros da rota (eventos GPS) -->
        <activity
            android:name=".RouteRecordsActivity"
            android:exported="false"
            android:parentActivityName=".StartRoute" />

        <!-- Lista de assignments (rotas disponíveis) -->
        <activity
            android:name=".AssignmentListActivity"
            android:exported="false"
            android:parentActivityName=".LoginPage" />

        <!-- Detalhes do assignment -->
        <activity
            android:name=".AssignmentDetailsActivity"
            android:exported="false"
            android:parentActivityName=".AssignmentListActivity" />

        <!-- Resumo da execução -->
        <activity
            android:name=".ExecutionSummaryActivity"
            android:exported="false"
            android:parentActivityName=".StartRoute" />

        <!-- Mapa do trajeto da execução -->
        <activity
            android:name=".ExecutionMapActivity"
            android:exported="false"
            android:parentActivityName=".ExecutionSummaryActivity" />

        <!-- Histórico de execuções -->
        <activity
            android:name=".ExecutionHistoryActivity"
            android:exported="false"
            android:parentActivityName=".AssignmentListActivity" />

        <!-- Histórico de execuções de uma rota específica -->
        <activity
            android:name=".AssignmentRouteHistoryActivity"
            android:exported="false"
            android:parentActivityName=".AssignmentDetailsActivity" />

        <!-- Serviço de rastreamento GPS -->
        <service
            android:name=".service.GpsTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>
</manifest>
