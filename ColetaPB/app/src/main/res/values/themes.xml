<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ColetaPB" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores primárias -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_blue_dark</item>
        <item name="colorOnPrimary">@color/text_white</item>
        
        <!-- Cores secundárias -->
        <item name="colorSecondary">@color/primary_blue_light</item>
        <item name="colorOnSecondary">@color/text_white</item>
        
        <!-- Cores de fundo -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/card_background</item>
        <item name="colorOnSurface">@color/text_primary</item>
        
        <!-- Cores de erro -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/text_white</item>
    </style>

    <style name="Theme.ColetaPB" parent="Base.Theme.ColetaPB" />
</resources>